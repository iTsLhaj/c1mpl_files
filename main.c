/* savefile_YYDDMM_hhmmss */
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <dirent.h>
#include <memory.h>


int main()
{

    // FILE *sf = fopen("point_t.dat", "w");
    // if (sf != NULL)
    // {
        
    //     fwrite(&pt, sizeof(point_t), 1, sf);
    //     fclose(sf);

    // }else
    //     fprintf(stderr, "Couldn't OpenFile!\n");
    
    // printf("Data is saved!\n");

    // FILE *sf = fopen("point_t.dat", "r");
    // if (sf != NULL)
    // {
        
    //     fread(&pt, sizeof(pt), 1, sf);
    //     fclose(sf);

    // }else
    //     fprintf(stderr, "Couldn't OpenFile!\n");
    

    return 0;

}